class Lobby {
    constructor(id, players, scores, state) {
        this.id = id;
        this.players = players;
        this.scores = scores;
        this.state = state;
    }
}

module.exports = Lobby;
